{
  "config": [
      {
          "name": "Docker",
          "filter": "\b(docker-compose.yml|docker-compose.yaml)\b"
      },
      {
          "terraform": "Terraform",
          "filter": "\b(.tf|.tf.json)\b"
      }
  ]
}